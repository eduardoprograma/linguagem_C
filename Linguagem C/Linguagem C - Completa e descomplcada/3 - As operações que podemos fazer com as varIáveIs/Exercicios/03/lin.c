/*Faça um programa que leia três valores inteiros e mostre sua soma*/
#include <stdio.h>

int main(void)
{
    int x = 1, y = 2, z = 3, resultado = 0;
    resultado = x + y + z;
    printf("\a");
    printf("\nA soma de %d, %d, %d e %d.\n\n", x, y, z, resultado);
    return 0;
}