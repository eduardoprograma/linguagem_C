/*Admita que i = 1, j = 2, k = 3 e m = 2. O que cada uma das seguintes instruções imprime? 
j) printf("%d", !(j > k));*/
#include <stdio.h>

int main(void)
{
    int j = 2, k = 3;
    printf("%d", !(j > k)); //1
    return 0;
}