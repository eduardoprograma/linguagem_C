/*Preencha as lacunas de cada uma das seguintes sentenças: 
    a) Todas as diretivas do pré-processador devem começar com_. 
    b) O bloco de compilação condicional pode ser ampliado para testar vários casos usando as diretivas _e_. 
    c) A diretiva_cria macros e constantes simbólicas. 
    d) Apenas caracteres_podem aparecer antes de uma diretiva de pré-processador em uma linha. 
    e) A diretiva_anula os nomes de constantes simbólicas e macros. 
    f) As diretivas _e _ são fornecidas como notação abreviada para #if defined(nome) e #if !defined(nome). 
    g) _permite ao programador controlar a execução das diretivas do préprocessador e a compilação do código do programa. 
    h) A macro_imprime uma mensagem e encerra a execução do programa se o valor da expressão que a macro calcula é 0. 
    i) A diretiva_insere um arquivo em outro arquivo. 
    j) O operador_concatena dois argumentos. 
    k) O operador_converte seu operando em uma string. 
    l) O caractere_indica que o texto de substituição de uma constante simbólica ou macro continua na próxima linha. 
    m) A diretiva_ faz com que as linhas do código-fonte sejam numeradas a partir do valor indicado começando na próxima linha do código-fonte.*/