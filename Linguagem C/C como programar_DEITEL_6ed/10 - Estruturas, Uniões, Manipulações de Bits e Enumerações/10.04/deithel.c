/*Encontre o erro em cada uma das instruções a seguir: 
    a) Assuma que struct carta foi definida contendo dois ponteiros de tipos char, que são face e naipe. Além disso, a variável c foi declarada como sendo do tipo struct carta e a variável cPtr foi declarada como sendo ponteiro para struct carta. O endereço de c foi atribuído à variável cPtr.
        printf("%s\n", *cPtr->face); 
    b) Assuma que struct carta foi definida contendo dois ponteiros para tipos char, que são face e naipe. Além disso, a variável copas [ 13 ] foi declarada do tipo struct carta. A instrução a seguir deve imprimir o membro face do elemento 10 do array.      
        printf("%s\n", copas.face);
    c) union valores { char w; float x; double y; } v = {1.27};
    d) struct pessoal { char sobreNome [15]; char primeiroNome[15]; int idade; }
    e) Assuma que struct pessoal foi definido como na parte (d) mas com a correção adequada. pessoal d; 
    f) Assuma que a variável p foi declarada do tipo struct pessoal e a variável c foi declarada do tipo struct carta. p = c;*/