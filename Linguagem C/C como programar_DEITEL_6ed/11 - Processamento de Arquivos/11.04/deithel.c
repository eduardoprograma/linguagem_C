/*Encontre o erro em cada um dos seguintes segmentos de programa. Explique como o erro pode ser corrigido. 
    a) O arquivo referenciado por f Ptr ("pagar. dat") não foi aberto. 
        fprintf(fPtr, "%ã%s%ã\n", conta, companhia, quantia);
    b) open("receber.dat", "r+"); 
    c) A instrução a seguir deve ler um registro do arquivo "pagar. dat". O ponteiro pagPtr se refere a esse arquivo e o ponteiro recPtr se refere ao arquivo " receber. dat". 
        fscanf(recPtr, "%d%s%d\n", fcconta, companhia, fcquantia);eliminação dos dados atuais. if ((tfPtr = fopenCobjetos.dat", "w") )
    d) O arquivo " ob j etos . dat" deve ser aberto para inclusão de dados no arquivo sem != NULL)
    e) O arquivo " cursos. dat" deve ser aberto para anexação sem modificação do conteúdo atual do arquivo.
        if ((cfPtr = fopenCcursos.dat", "w+"))!= NULL)*/