/*Repetição controlada por contador*/
#include <stdio.h>
#include <stdlib.h>

main () {
    int contador;
    /*Inicialização condição do incremento*/
    /*Estão incluidos no cabeçario do incremento*/
    
    for(contador = 0; contador <= 10; contador++)
    {
        printf("%d\n", contador);
    }
    system("pause");
    return 0;
}