#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
main() {
    int x, y, contador, potencia;

    contador = 1;
    potencia = 1;

    printf("Digite um valor: ");
    scanf("%d", x);
    printf("Digite um valor: ");
    scanf("%d", x);
    printf("O incremento de y e: %d\n", y += 1);
    potencia = potencia * x;
    printf("A potencia e: %d\n", potencia);
    return 0;
}