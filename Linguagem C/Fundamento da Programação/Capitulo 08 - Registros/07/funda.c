/*Considere que exista um registro com os seguintes atributos: codigo_cliente e nome_cliente e um outro registro cmo os seguintes atributos: N_conta, valor_compra, codigo_cliente. Faça um programa que:
    °   inclua clientes, não permitindo assim que dois  clientes possuam o mesmo código;
    °   inclua contas, verificando se o código  do cliente informado já está cadastrado;
    °   remova um determinado cliente. Antes de executar executar a remoção, verifique se o cliente  possui alguma compra. Se possuir, mostrar a mensagem Exclusão não permitida. Caso contrário, proceder à exclusão.*/